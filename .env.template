# Copy this file to .env. Never commit the generated .env file.
# Generate a new value with: python3 -c 'import secrets; print(secrets.token_urlsafe(50))'
SECRET_KEY=replace-with-a-unique-random-secret

# Set True for local debugging only.
DEBUG=False

# Comma-separated hostnames or IP addresses used to access the application.
ALLOWED_HOSTS=localhost,127.0.0.1
